# ExplicitGraphDag

Builds a two-node directed acyclic graph, inspects its managed topology, instantiates it, and launches it on a stream.

```powershell
dotnet run --project samples/tutorials/05-Graph/ExplicitGraphDag/ExplicitGraphDag.csproj -c Release
```
