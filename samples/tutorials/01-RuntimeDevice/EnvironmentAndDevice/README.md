# EnvironmentAndDevice / 环境与设备

Initializes HIP, prints the runtime and driver versions, and enumerates device ordinals and names. Run only on a machine with a working AMD HIP Runtime:

```bash
dotnet run --project samples/tutorials/01-RuntimeDevice/EnvironmentAndDevice/EnvironmentAndDevice.csproj -c Release
```
