# LoaderDiagnostics

Shows the native-library loading diagnostics produced when `amdhip64` is found or missing.

运行：

```powershell
dotnet run --project samples/tutorials/01-RuntimeDevice/LoaderDiagnostics/LoaderDiagnostics.csproj -c Release
```
