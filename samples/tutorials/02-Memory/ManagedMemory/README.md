# ManagedMemory

Demonstrates `HipManagedMemory`, host/device visibility, usage advice, prefetch, and synchronization.

运行：

```powershell
dotnet run --project samples/tutorials/02-Memory/ManagedMemory/ManagedMemory.csproj -c Release
```
