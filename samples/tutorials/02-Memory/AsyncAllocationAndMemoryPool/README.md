# AsyncAllocationAndMemoryPool

Demonstrates stream-ordered allocation, asynchronous release, a custom memory pool, and owner lifetime ordering.

运行：

```powershell
dotnet run --project samples/tutorials/02-Memory/AsyncAllocationAndMemoryPool/AsyncAllocationAndMemoryPool.csproj -c Release
```
