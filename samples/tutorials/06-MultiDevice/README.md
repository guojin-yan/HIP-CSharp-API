# Multi-device

This module introduces explicit device pairs, current-device requirements, capability checks, and
peer memory copies.

本模块介绍显式设备对、当前设备约束、Capability 检查和 Peer Memory Copy。

- [`PeerToPeerCopy`](PeerToPeerCopy)
