# KernelOccupancy

Compiles a tiny kernel and queries occupancy and the suggested launch plan. Pass the target GPU architecture as the first argument.

```powershell
dotnet run --project samples/tutorials/04-Kernel/KernelOccupancy/KernelOccupancy.csproj -c Release -- gfx1100
```
