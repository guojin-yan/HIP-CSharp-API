# ModuleGlobals

Compiles a module-owned device global and demonstrates the typed borrowed global view.

```powershell
dotnet run --project samples/tutorials/04-Kernel/ModuleGlobals/ModuleGlobals.csproj -c Release -- gfx1100
```
