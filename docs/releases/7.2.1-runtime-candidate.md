# Linux runtime 7.2.1 candidate / Linux runtime 7.2.1 候选

This document covers the guarded Linux Runtime line, not a published NuGet release. The renamed package ID is `JYPPX.ROCm.HipSharp.Runtime.linux-x64`; the package version remains the ROCm version `7.2.1`. Its M8.7 exact candidate passed fresh official-host and package-only validation. M8.8 permits a local verified-final package only through the matching promotion receipt; it remains `publishable=false` and `releaseAuthorized=false`.

本文件描述受控的 Linux Runtime 包线，不代表已发布 NuGet。重命名后的包 ID 为 `JYPPX.ROCm.HipSharp.Runtime.linux-x64`，包版本仍使用 ROCm 版本 `7.2.1`。其 M8.7 精确候选已通过新的 official-host 与 package-only 验证；M8.8 仅允许匹配 promotion receipt 的本地 verified-final 包，并继续保持 `publishable=false` 与 `releaseAuthorized=false`。

Locally completed / 本地已完成：

- AMD Noble `InRelease` OpenPGP verification and exact package/file SHA-256 locks.
- HIP Runtime, HIPRTC/builtins, HSA Runtime, COMGR, and rocprofiler-register dependency closure.
- Ubuntu system-library and AMD kernel/device-node boundaries.
- MIT, NCSA, and Apache-2.0 WITH LLVM-exception license inventory.
- Deterministic CycloneDX 1.5 SBOM and provenance/dependency/license/size reports.
- 415,070,520-byte unpacked allowlist; the former package ID historically produced a 162,891,900-byte candidate and 162,892,126-byte verified final package, so the single-package topology is retained without treating those values as verification of the renamed package.
- Fail-closed staging, pack, and package-audit entry points; Windows remains disabled.

M8.7 exact-candidate gate / M8.7 精确候选门禁：

- A newly Owner-authorized Ubuntu Base consumer with `/dev/kfd` and `/dev/dri`, but no system ROCm user-mode libraries.
- Core/runtime package audits, loader/readelf/ldd/process maps, managed Runtime `91/91`, managed HIPRTC `9/9`, complete Runtime `458/459`, complete HIPRTC `18/18`, and schema-7 ABI.
- DeviceInfo, MemoryCopy, HIPRTC VectorAdd, stream/event VectorAdd, all five managed expansion stages with 1,127 comparisons, bounded reliability, and missing-HSA/core-only/tampered/mixed-closure negatives.

The source manifest is promoted to `packEnabled=true` and `verified=true` only because its validation hash identifies the deterministic M8.7 receipt. Final packing also requires that tracked receipt at the MSBuild boundary; direct, incomplete, forged, stale, or Windows runtime packs remain fail-closed. The new final Core/Runtime bytes still require newly Owner-authorized official-host and PRoot final-mode gates before release-ready status. Single-device P2P remains a skip, reliability is not a benchmark, and no broad support claim is made.
