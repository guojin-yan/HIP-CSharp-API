# Ubuntu 24.04 Runtime 7.2.1 candidate / Ubuntu 24.04 Runtime 7.2.1 候选

This document covers `JYPPX.ROCm.HIP.CSharp.API.Runtime.ubuntu.24.04-x64` version `7.2.1`. The package is not published. Its manifest remains `packEnabled=false` and `verified=false` until the exact distribution-specific nupkg passes content, size, provenance, clean-consumer, and GPU/ABI validation.

本文件描述 `JYPPX.ROCm.HIP.CSharp.API.Runtime.ubuntu.24.04-x64` `7.2.1`。该包尚未发布；在发行版专用 nupkg 完成内容、体积、来源、clean consumer 与 GPU/ABI 精确验证前，manifest 保持 `packEnabled=false`、`verified=false`。

Reusable source evidence / 可复用的源证据：

- AMD Noble `InRelease` OpenPGP verification and exact source package/file SHA-256 locks.
- HIP Runtime, HIPRTC/builtins, HSA Runtime, COMGR, and rocprofiler-register dependency closure.
- Ubuntu system-library and AMD kernel/device-node boundaries.
- MIT, NCSA, and Apache-2.0 WITH LLVM-exception license inventory.
- Deterministic CycloneDX 1.5 SBOM and provenance/dependency/license/size reports.
- 415,070,520-byte unpacked allowlist and fail-closed staging, pack, and package-audit entry points.

Required promotion evidence / 晋级前必需证据：

- Exact nupkg size and SHA-256 bound to the Ubuntu 24.04 package ID and nuspec.
- Isolated Ubuntu 24.04 consumer with `/dev/kfd` and `/dev/dri`, without system ROCm user-mode fallback.
- Loader/readelf/ldd/process maps, managed and complete symbol coverage, schema-7 ABI, and representative workloads.
- Missing-HSA, core-only, tampered-package, and mixed-closure negatives.
- A new tracked promotion lock and receipt whose hash becomes `verification.validationSha256`.

Direct, incomplete, forged, or stale runtime packs remain fail-closed. Single-device P2P remains an honest skip when unsupported, reliability is not a benchmark, and no broad Linux support claim is made.
Once all gates pass, the `runtime-ubuntu.24.04-v7.2.1` annotated tag triggers publication to NuGet.org, repository-signature verification, and upload of the nupkg plus SHA-256 to GitHub Releases.
