# Linux runtime 7.2.1 candidate / Linux runtime 7.2.1 候选

This is a blocked local candidate design, not a published NuGet release. The public package ID remains `JYPPX.HipSharp.Runtime.linux-x64`; the package version is the ROCm version `7.2.1`.

本文件描述被门禁阻断的本地候选设计，不代表已发布 NuGet。公开包 ID 保持 `JYPPX.HipSharp.Runtime.linux-x64`，包版本使用 ROCm 版本 `7.2.1`。

Locally completed / 本地已完成：

- AMD Noble `InRelease` OpenPGP verification and exact package/file SHA-256 locks.
- HIP Runtime, HIPRTC/builtins, HSA Runtime, COMGR, and rocprofiler-register dependency closure.
- Ubuntu system-library and AMD kernel/device-node boundaries.
- MIT, NCSA, and Apache-2.0 WITH LLVM-exception license inventory.
- Deterministic CycloneDX 1.5 SBOM and provenance/dependency/license/size reports.
- 415,070,520-byte unpacked allowlist and 162,813,488-byte preflight ZIP; single-package topology retained.
- Fail-closed staging, pack, and package-audit entry points; Windows remains disabled.

Still required / 仍需完成：

- A final nupkg content audit and exact nupkg SHA-256/size.
- A newly Owner-authorized isolated GPU consumer with no system ROCm user-mode libraries.
- Loader/readelf/ldd/process-map evidence and the full Runtime/Memory/Stream/Event/Module/HIPRTC GPU gate.

Until those gates pass, `packEnabled=false`, `verified=false`, and `HIPSHARP1001` prevents package generation.
