# Linux runtime 7.2.1 candidate / Linux runtime 7.2.1 候选

This is a verified guarded local candidate, not a published NuGet release. The public package ID remains `JYPPX.HipSharp.Runtime.linux-x64`; the package version is the ROCm version `7.2.1`.

本文件描述已通过门禁的受控本地候选，不代表已发布 NuGet。公开包 ID 保持 `JYPPX.HipSharp.Runtime.linux-x64`，包版本使用 ROCm 版本 `7.2.1`。

Locally completed / 本地已完成：

- AMD Noble `InRelease` OpenPGP verification and exact package/file SHA-256 locks.
- HIP Runtime, HIPRTC/builtins, HSA Runtime, COMGR, and rocprofiler-register dependency closure.
- Ubuntu system-library and AMD kernel/device-node boundaries.
- MIT, NCSA, and Apache-2.0 WITH LLVM-exception license inventory.
- Deterministic CycloneDX 1.5 SBOM and provenance/dependency/license/size reports.
- 415,070,520-byte unpacked allowlist and 162,891,900-byte validated candidate; single-package topology retained.
- Fail-closed staging, pack, and package-audit entry points; Windows remains disabled.

Completed isolated gate / 已完成隔离门禁：

- A newly Owner-authorized Ubuntu Base consumer with `/dev/kfd` and `/dev/dri`, but no system ROCm user-mode libraries.
- Core/runtime package audits, loader/readelf/ldd/process maps, 31 Runtime exports, and 9 HIPRTC exports.
- DeviceInfo, MemoryCopy, HIPRTC VectorAdd, stream/event VectorAdd, and missing-HSA/core-only/tampered/mixed-closure negatives.

`packEnabled=true` and `verified=true` allow only the guarded final pack path. The final nupkg is rebuilt from a clean post-evidence SHA and must pass the same isolated gate; direct incomplete or Windows runtime packs remain fail-closed.
