# Linux Runtime 7.2.1 published state / Linux Runtime 7.2.1 已发布状态

This document covers the guarded Linux Runtime line. The corrected package-family ID `JYPPX.ROCm.HIP.CSharp.API.Runtime.linux-x64` version `7.2.1` is published and passed repository-signature, protected-entry equivalence, nuget.org-only static, and package-only GPU/ABI validation. The old ID `JYPPX.ROCm.HipSharp.Runtime.linux-x64` `7.2.1` is immutable, unlisted history.

本文件描述受控的 Linux Runtime 包线。修正后的 `JYPPX.ROCm.HIP.CSharp.API.Runtime.linux-x64` `7.2.1` 已公开，并通过 repository signature、protected-entry equivalence、nuget.org-only 静态与 package-only GPU/ABI 验证。旧 ID `JYPPX.ROCm.HipSharp.Runtime.linux-x64` `7.2.1` 是不可变且已 unlist 的历史。

Locally completed / 本地已完成：

- AMD Noble `InRelease` OpenPGP verification and exact package/file SHA-256 locks.
- HIP Runtime, HIPRTC/builtins, HSA Runtime, COMGR, and rocprofiler-register dependency closure.
- Ubuntu system-library and AMD kernel/device-node boundaries.
- MIT, NCSA, and Apache-2.0 WITH LLVM-exception license inventory.
- Deterministic CycloneDX 1.5 SBOM and provenance/dependency/license/size reports.
- 415,070,520-byte unpacked allowlist; the corrected public signed package is 162,907,424 bytes with SHA-256 `21D0A2E511964923DE4BE2C7F1BF02CE19E9ABD9E9BF535CB915C7D7C81B5799`.
- Fail-closed staging, pack, and package-audit entry points; Windows remains disabled.

M8.7 exact-candidate gate / M8.7 精确候选门禁：

- A newly Owner-authorized Ubuntu Base consumer with `/dev/kfd` and `/dev/dri`, but no system ROCm user-mode libraries.
- Core/runtime package audits, loader/readelf/ldd/process maps, managed Runtime `91/91`, managed HIPRTC `9/9`, complete Runtime `458/459`, complete HIPRTC `18/18`, and schema-7 ABI.
- DeviceInfo, MemoryCopy, HIPRTC VectorAdd, stream/event VectorAdd, all five managed expansion stages with 1,127 comparisons, bounded reliability, and missing-HSA/core-only/tampered/mixed-closure negatives.

The source manifest is `packEnabled=true` and `verified=true` only because its validation hash identifies the tracked M8.9 promotion receipt. Final packing still requires that receipt at the MSBuild boundary; direct, incomplete, forged, stale, or Windows runtime packs remain fail-closed. Pairing this immutable public Runtime with a different Core `1.0.0` package does not transfer GPU authorization to the new Core bytes. Single-device P2P remains a skip, reliability is not a benchmark, and no broad support claim is made.
