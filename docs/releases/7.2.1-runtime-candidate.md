# Linux runtime 7.2.1 candidate / Linux runtime 7.2.1 候选

This document covers the guarded Linux Runtime line, not a published NuGet release. The renamed package ID is `JYPPX.ROCm.HipSharp.Runtime.linux-x64`; the package version remains the ROCm version `7.2.1`. M5 produced a verified historical package under the former ID. The renamed line requires a distinct exact-SHA candidate with `verified=false` and `publishable=false` until fresh isolated validation.

本文件描述受控的 Linux Runtime 包线，不代表已发布 NuGet。重命名后的包 ID 为 `JYPPX.ROCm.HipSharp.Runtime.linux-x64`，包版本仍使用 ROCm 版本 `7.2.1`。M5 在旧 ID 下生成了已验证历史包；新命名包线必须生成不同的精确 SHA 候选，在新隔离验证前保持 `verified=false` 和 `publishable=false`。

Locally completed / 本地已完成：

- AMD Noble `InRelease` OpenPGP verification and exact package/file SHA-256 locks.
- HIP Runtime, HIPRTC/builtins, HSA Runtime, COMGR, and rocprofiler-register dependency closure.
- Ubuntu system-library and AMD kernel/device-node boundaries.
- MIT, NCSA, and Apache-2.0 WITH LLVM-exception license inventory.
- Deterministic CycloneDX 1.5 SBOM and provenance/dependency/license/size reports.
- 415,070,520-byte unpacked allowlist; the former package ID historically produced a 162,891,900-byte candidate and 162,892,126-byte verified final package, so the single-package topology is retained without treating those values as verification of the renamed package.
- Fail-closed staging, pack, and package-audit entry points; Windows remains disabled.

Historical isolated gate / 历史隔离门禁：

- A newly Owner-authorized Ubuntu Base consumer with `/dev/kfd` and `/dev/dri`, but no system ROCm user-mode libraries.
- Core/runtime package audits, loader/readelf/ldd/process maps, the original 31 Runtime + 9 HIPRTC gate, and the later M6 46 Runtime + 9 HIPRTC historical regression.
- DeviceInfo, MemoryCopy, HIPRTC VectorAdd, stream/event VectorAdd, M6 advanced/stress paths, and missing-HSA/core-only/tampered/mixed-closure negatives.

The renamed base manifest is deliberately `packEnabled=false` and `verified=false`. Its source, closure, license, and SBOM locks describe the unchanged payload, while former package-ID audits and GPU results remain historical evidence only. Candidate generation derives a separate manifest and attestation from the current clean commit. The current exact Core/Runtime pair is pending newly Owner-authorized host and PRoot gates; direct final, incomplete, or Windows runtime packs remain fail-closed.
