# M8.10 controlled cleanup record

Inventory date: 2026-08-15. All targets were resolved to explicit absolute paths below the program repository before deletion. No workspace root, planning directory, diary, Radeon Cloud record, tracked source, manifest, SBOM, license, promotion receipt, release note or user modification was included.

| Category | Before | Purpose | Rebuild / recovery | Decision |
| --- | ---: | --- | --- | --- |
| `artifacts/` | 2,035 files; 4,804,870,270 bytes | Historical packages, bundles, consumers, audits, test outputs and native copies | Source commits/tags remain in Git; public `0.9.1`/`7.2.1` packages are retrievable by locked public hashes; necessary M8.9 hashes/commands/results are in long-term redacted records | remove; no exact M8.9 upload or public signed package bytes were present |
| `eng/native-assets/cache/` | 126 files; 53,078,761 bytes | Download/tool cache | `prepare-runtime.ps1` can reacquire from manifest-pinned AMD signed URLs/hashes; offline rebuild costs a new download | remove |
| `eng/native-assets/staging/` | 95 files; 1,077,060,057 bytes | Extracted runtime staging and aliases | Deterministically rebuilt from the tracked manifest/SBOM and signed source packages; contains no unique source | remove |
| all project `bin/`, `obj/`, root empty `bin/obj`, Python `__pycache__` | 2,638 files; 500,940,957 bytes | Build/test/tool outputs | `dotnet restore/build/test`, runtime preparation and probe scripts | remove |
| `.vs/` | 15 files; 3,634,599 bytes | IDE state | Visual Studio recreates it | remove |
| `_site/` and `docs/api/` | 369 files; 22,629,873 bytes | Generated DocFX site/metadata, including stale legacy namespace pages | `eng/docs.ps1`; both paths remain Git-ignored | remove through the new clean-generation entry |
| stale `HIP-CSharp-API-m89-final` worktree metadata | directory already absent | Historical detached release checkout at `63f33cf...` | Commit and immutable `v0.9.1` tag remain in the main repository | `git worktree prune`; never direct metadata deletion |
| nested `artifacts/fresh-checkout-2a89c67` | clean detached `2a89c676...`; included in `artifacts/` total | M8.8 bundle checkout | Commit and immutable `v0.9.0` tag remain in the main repository; its bundle was only a transport duplicate | remove with `artifacts/` after clean/status verification |
| six tracked `.gitkeep` files | 6 bytes | Earlier skeleton placeholders | Git tracks populated directories without placeholders; empty `Events` and `Runtime` had no references | remove all six |

Planned recoverable space was 6,462,214,517 bytes before rebuild. Subsequent local gates recreate only current `1.0.0` outputs. Exact post-clean and final post-candidate sizes are recorded in the M8.10 diary/record so regenerated evidence is not confused with historical cache.

Retained evidence: tracked runtime manifests, source locks, component licenses, CycloneDX SBOM, M8.9 promotion lock/receipt, immutable tags and Git history, public package hashes, release notes, and all long-term `plan/`, `diary/`, and `Radeon_Cloud/records/` material. Large native/package bytes are intentionally not committed.
